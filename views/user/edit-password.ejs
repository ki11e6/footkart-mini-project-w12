<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>

    <body class="d-flex flex-column min-vh-100">
        <%- include('../partials/top-navbar')%>

            <div class="container-fluid d-flex justify-content-center ">
                <div class="row mt-5 bg-dark rounded-4">
                    <div class="col-sm-6 text-white p-4">
                        <form class="OTP" id="OTP" action="/edit-password" method="POST">

                            <div class="input-control mb-4 ">
                                <label class="form-label fw-bold" for="newpassword">Enter New
                                    Password</label>
                                <input type="text" id="newpassword" name="newpassword"
                                    class="form-control form-control-lg" />

                            </div>


                            <div class="mb-4 d-flex justify-content-center  btn-lg">
                                <button class=" btn login-btn m-auto px-5 py-1 rounded-pill fs-3 fw-bold" type="submit"
                                    name="submit" id="submit">Add password</button>
                            </div>

                        </form>
                        <div></div>
                        <div></div>

                    </div>
                </div>
            </div>


            <%- include('../partials/bottom-navbar') %>
                <!-- <script src="script/script.js"></script>
    <script>var timeLeft = 10;
        var elem = document.getElementById('time');
        var timerId = setInterval(countdown, 1000);
        
        function countdown() {
            var button = document.getElementById("timer");
            if (timeLeft == -1) {
              button.style.display = "inline-block";
                
            } else {
                elem.innerHTML = 'Resent otp in ' + timeLeft + ' seconds';
                timeLeft--;
            }
        } -->
                </script>

    </body>

</html>